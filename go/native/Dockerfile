# 기본 이미지로 Go를 사용합니다.
FROM golang:alpine

# 작업 디렉토리를 설정합니다.
WORKDIR /app

# 의존성 파일을 먼저 복사합니다.
# (이 예제에서는 의존성 관리 파일이 없으므로 생략 가능합니다.)

# 소스 코드를 작업 디렉토리로 복사합니다.
COPY . .

# Go 언어로 애플리케이션을 빌드합니다.
RUN go build -o main .

# 컨테이너 실행 시 실행될 명령어를 지정합니다.
CMD ["./main"]
